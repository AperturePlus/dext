{
  "version": "generation-v1",
  "grounded_rules_manifest_hash": "23531ed9edf058fedff7c7d8e04cdad046a163481dee0b0d81b6e7d07996b625",
  "operations": {
    "query_understanding": {
      "system_prompt_id": "dext_recommend.query_understanding.v1",
      "system_prompt": "Extract the student's mentor-search constraints. Return only JSON matching the supplied schema. Do not invent professor facts or infer sensitive attributes.",
      "json_schema": {
        "type": "object",
        "additionalProperties": false,
        "required": ["research_interests", "preferred_universities", "preferred_cities", "preferred_org_units", "degree_goal", "mentor_eligibility_requirement", "missing_information", "needs_clarification", "confidence"],
        "properties": {
          "research_interests": {"type": "array", "items": {"type": "string"}},
          "preferred_universities": {"type": "array", "items": {"type": "string"}},
          "preferred_cities": {"type": "array", "items": {"type": "string"}},
          "preferred_org_units": {"type": "array", "items": {"type": "string"}},
          "degree_goal": {"type": ["string", "null"]},
          "mentor_eligibility_requirement": {"type": ["string", "null"]},
          "missing_information": {"type": "array", "items": {"type": "string"}},
          "needs_clarification": {"type": "boolean"},
          "confidence": {"type": "number", "minimum": 0.0, "maximum": 1.0}
        }
      },
      "timeout": 8.0,
      "token_budget": 1024
    },
    "implicit_intent": {
      "system_prompt_id": "dext_recommend.implicit_intent.v1",
      "system_prompt": "Classify the follow-up intent using only the supplied context. Return only JSON matching the supplied schema. Never reveal rationale outside the structured field.",
      "json_schema": {
        "type": "object",
        "additionalProperties": false,
        "required": ["intent", "confidence", "rationale"],
        "properties": {
          "intent": {"enum": ["new_search", "more_mentors", "same_field", "refine_direction", "detail_followup"]},
          "confidence": {"type": "number", "minimum": 0.0, "maximum": 1.0},
          "rationale": {"type": "string", "maxLength": 200}
        }
      },
      "timeout": 8.0,
      "token_budget": 1024,
      "confidence_threshold": 0.6,
      "query_max_chars": 4096,
      "summary_max_chars": 500
    },
    "detail_followup": {
      "system_prompt_id": "dext_recommend.detail_followup.v1",
      "system_prompt": "Answer only from the supplied fact bundle. Every factual claim must cite the supporting fact indices and refs. Do not provide contacts or probability claims.",
      "json_schema": {
        "type": "object",
        "additionalProperties": false,
        "required": ["answer", "claims"],
        "properties": {
          "answer": {"type": "string"},
          "claims": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["text", "content_class", "fact_indices", "fact_refs"],
              "properties": {
                "text": {"type": "string"},
                "content_class": {"enum": ["fact", "advice", "uncertain"]},
                "fact_indices": {"type": "array", "items": {"type": "integer"}},
                "fact_refs": {"type": "array"}
              }
            }
          }
        }
      },
      "timeout": 15.0,
      "token_budget": 2048
    },
    "match_analysis": {
      "system_prompt_id": "dext_recommend.match_analysis.v1",
      "system_prompt": "Analyze mentor-student fit only from the supplied fact bundle and authorized student context. Return JSON only. Dimension scores are explanatory fit scores, never admission probability. Every factual claim must cite fact_indices and fact_refs.",
      "json_schema": {
        "type": "object",
        "additionalProperties": false,
        "required": ["summary", "dimension_scores", "next_steps", "claims"],
        "properties": {
          "summary": {"type": "string"},
          "dimension_scores": {"type": "object"},
          "next_steps": {"type": "array", "items": {"type": "string"}},
          "claims": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["text", "content_class", "fact_indices", "fact_refs"],
              "properties": {
                "text": {"type": "string"},
                "content_class": {"enum": ["fact", "advice", "uncertain"]},
                "fact_indices": {"type": "array", "items": {"type": "integer"}},
                "fact_refs": {"type": "array"},
                "user_context_ref": {"type": ["object", "null"]}
              }
            }
          }
        }
      },
      "timeout": 15.0,
      "token_budget": 2048
    },
    "outreach_email": {
      "system_prompt_id": "dext_recommend.outreach_email.v1",
      "system_prompt": "Draft an editable outreach email from the supplied fact bundle and authorized student context. Do not send email, invent experience, include contacts, or make admission/probability claims. Return JSON only.",
      "json_schema": {
        "type": "object",
        "additionalProperties": false,
        "required": ["subject", "body", "claims"],
        "properties": {
          "subject": {"type": "string"},
          "body": {"type": "string"},
          "claims": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["text", "content_class", "fact_indices", "fact_refs"],
              "properties": {
                "text": {"type": "string"},
                "content_class": {"enum": ["fact", "advice", "uncertain"]},
                "fact_indices": {"type": "array", "items": {"type": "integer"}},
                "fact_refs": {"type": "array"},
                "user_context_ref": {"type": ["object", "null"]}
              }
            }
          }
        }
      },
      "timeout": 15.0,
      "token_budget": 2048
    },
    "professor_comparison": {
      "system_prompt_id": "dext_recommend.professor_comparison.v1",
      "system_prompt": "Compare 2-3 professors only from the merged fact bundle and authorized student context. Explicitly mark evidence gaps. Do not hide insufficient evidence or make admission/probability claims. Return JSON only.",
      "json_schema": {
        "type": "object",
        "additionalProperties": false,
        "required": ["summary", "professor_notes", "evidence_gaps", "claims"],
        "properties": {
          "summary": {"type": "string"},
          "professor_notes": {"type": "object"},
          "evidence_gaps": {"type": "object"},
          "claims": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["text", "content_class", "fact_indices", "fact_refs"],
              "properties": {
                "text": {"type": "string"},
                "content_class": {"enum": ["fact", "advice", "uncertain"]},
                "fact_indices": {"type": "array", "items": {"type": "integer"}},
                "fact_refs": {"type": "array"},
                "user_context_ref": {"type": ["object", "null"]}
              }
            }
          }
        }
      },
      "timeout": 20.0,
      "token_budget": 3072
    }
  }
}
